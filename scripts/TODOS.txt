- Need a way to force rebalance unbounded growth tasks when new tasks are scheduled (if new tasks don't have room). Manager should ask unbounded tasks to shut down, wait for them, then re-plan allocation (with pending tasks + refreshed unbound task)
- When available server capacity changes dramatically (can probably identify by amount of total server capacity exceeding the task manager's task allocations, IF there are unbound tasks), we should show an o ption in the task panel UX to re-calculate available space and re-plan unbound tasks on the new space.
- Obviously, need to work on a trading task. Probably safe to assume full market access instead of increments, we'll just wait to trade until we have the whole thing (??)
- Add 'search' in the server map so we can quickly search for a server to jump to it
- Add a "status" (weaken/grow/hack) to the noform hacker so we can display it in the task panel
